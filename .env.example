# Neon Postgres — pooled connection (runtime queries)
DATABASE_URL=

# Neon Postgres — direct/non-pooled connection (used only by `prisma migrate`)
DIRECT_URL=

# Auth0 (@auth0/nextjs-auth0 v4)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
APP_BASE_URL=http://localhost:3000
